# 📋 无人机航拍浮游植物智能识别与水华预警系统 —— 团队主README

## 🎯 1. 项目概述

本项目针对湖泊/水库水华（浮游植物大量繁殖）爆发监测的现实需求，搭建 **「无人机数据采集 → 图像预处理与标注 → AI视觉分割（天气自适应） → 数模时序预测 → 交互式可视化展示」** 全链路系统。核心解决传统水华监测成本高、频次低、数据滞后、精准度不足四大痛点，实现浮游植物精准识别、时空分布量化、未来趋势预警与成果直观呈现。

**最终产品形态**：一个统一的 Web 端应用，用户可创建自己的“池塘”监测点，按时间上传原始航拍图与无人机参数，系统自动完成校正、分割、量化与预测，并通过动态时间轴展示该池塘的浮游植物变化。大创阶段以完成技术闭环、落地本地可演示 Demo 为核心验收目标，不追求公网部署、每日高频采集等过度工程化内容；长期可拓展为可落地的水体监测平台，支撑环保部门水质安全管理与决策。

## 🎯 2. 项目核心目标

### 2.1 大创阶段核心目标（1年内验收标准）

1. **数据层**：完成高质量无人机浮游植物数据集构建，含：
   - 原始航拍图像及配套无人机飞行参数（高度、姿态、相机参数等）；
   - 基于无人机参数的几何校正（正射校正）脚本及执行；
   - 校正后图像的像素级 LabelMe 标注。
   数据无错标、漏标、格式混乱，可直接用于深度学习模型训练与面积量化。
   **数据采集说明**：
   - **训练数据集**：用于模型训练的标注数据不要求所有图像均拍摄于同一位置，可混合不同水域的图像以提升模型泛化能力。
   - **演示实测数据**：用于最终展示与数模预测基础的连续时序数据，需尽量锁定同一目标水体（同一池塘），保持每次飞行覆盖该水体且中心GPS与高度基本一致，以确保多次监测面积值构成可比的时序序列，但不要求图像间的严格像素对齐。

2. **算法层**：基于天气分类网络 + 多条件 DeepLabV3+ 实现浮游植物像素级语义分割，达成四大核心功能：① 精准识别有无浮游植物聚集；② 定位其空间分布；③ 区分微囊藻（蓝藻）、水华鱼腥藻（蓝藻）、水绵（绿藻）及其他常见浮游植物；④ 量化浮游植物覆盖面积、覆盖率、浓度等级（高/中/低）。
   - **天气自适应分割策略（含对比验证）**：由于浮游植物多类别分割依赖细微色彩差异，不同天气条件下光照、散射、雾霾导致的色偏会严重干扰模型判断。为此引入前置天气分类模型（识别晴天/阴天/雾天），并分别训练三个专用的 DeepLabV3+ 分割模型。**同时训练一个不区分天气的统一 DeepLabV3+ 模型作为基线**，在相同测试集上与天气自适应策略进行量化对比（mIoU、Accuracy），以客观验证天气自适应策略的有效性，并分析不同天气下模型表现的差异。
   - **模型只做分割，面积在后处理中计算**：语义分割模型仅输出像素级类别掩码，不直接输出面积。推理管道 `predict_pipeline.py` 将读取校正图像的元数据（含 GSD）完成实际面积换算，保证科学严谨性。
   - **天气分类模型数据来源**：天气分类属于通用视觉任务，使用 **多类天气图片数据集**（深圳大学VCC发布，和鲸社区提供镜像）进行训练。选用其中 `sunny`（晴天）、`cloudy`（阴天/多云）、`haze`（雾天）三类，每类约 10,000 张图片。训练数据存放于 `data/weather_public/` 对应文件夹，无需占用本项目标注人力。

3. **数模层**：基于 AI 视觉输出的时序实测面积数据，搭建**双模型预测系统**，输出未来 1-2 个月浮游植物面积变化与水华风险等级：
   - **统计基线模型（ARIMA）**：仅依赖历史面积时序数据进行统计拟合与预测，适用于无环境监测条件的普通用户。
   - **机理增强模型（Logistic 生长方程 + 环境驱动）**：基于藻类种群生长的常微分方程，引入水温、光照等环境变量修正生长速率。方程中的关键参数（最大生长速率、半饱和常数等）使用实测面积和环境数据通过**非线性最小二乘法拟合**确定。对未上传的环境参数，系统提供基于文献和地区气候统计数据的**科学默认值**，确保模型在无实测环境数据时也能合理运行。
   - 预测数据格式与实测数据完全对齐。**数模仅输出纯数值预测结果，不直接生成图像**；未来各日期的预测分布图由数模组调用 AI 模块提供的**增强版空间仿真脚本**生成。该脚本不仅接收面积变化率，还能接收从实测序列中提取的**空间生长规则**（如优先从斑块边缘扩张），使预测掩码的空间分布更真实。数模组调用该脚本产出仿真掩码图，按约定命名直接存入 `outputs/masks/`。

4. **展示层**：开发本地可运行交互式可视化 Demo，实现核心交互：① 拖动时间进度条切换不同日期；② 一键切换“实测模式/预测模式”查看浮游植物分布图；③ 同步展示面积、覆盖率、浓度等级等量化数据；④ 提供**预测模型选择与参数配置界面**，支持简易模式（纯统计）和精准模式（机理模型+可选参数）。演示采用「3-5 组真实实测数据 + 模拟预测数据」，无需每日拍摄更新。
   **产品模拟**：Demo 拟实现“我的池塘”管理界面，支持创建池塘、按日期上传原始图像与无人机参数，模拟最终用户的使用流程。

5. **协同层**：各模块数据对接顺畅、分工边界清晰，全流程可现场操作演示，满足结题答辩验收要求。

### 2.2 长期拓展目标（1年后进阶方向）

1. **数据层**：支持周期性（每周/每月）无人机数据更新，实现模型自动迭代优化，提升识别与预测精准度。
2. **算法层**：优化模型鲁棒性，适配水草、阴影、强光反光、雾霾等复杂水体环境；拓展更多浮游植物种类识别；扩充天气类型与对应模型库；探索更高效的 Transformer 类分割模型。
3. **展示层**：新增“新图片一键识别预测”功能，支持用户上传新航拍图获取结果；支持局域网部署，满足定点监测使用需求。
4. **应用层**：对接环保部门数据平台，实现监测数据同步上报，支撑实际水质管理工作。

## 👥 3. 技术模块与分工边界

项目分为四大核心模块，分工严格隔离、数据单向流转、无交叉重叠工作，各模块仅负责自身职责，通过标准化文件完成对接。

### 🗂️ 3.1 数据采集与预处理标注模块（负责人：贺一冉）

**核心定位**：项目数据源头，负责采集原始图像与配套参数，运行几何校正脚本输出标准正射图像，并完成像素级标注，为 AI 模块提供可直接训练/推理的数据。

**具体职责**

- **数据采集配合**：对接航拍组，明确拍摄规范（垂直90°俯拍、相机使用原生默认焦距（1x），禁止光学/数字变焦，锁定“日光”预设（或相应天气预设如“阴天”），禁止自动白平衡，M档RAW、10-50m高度、覆盖晴天/阴天/雾天）。**采集训练数据时，可不限定同一拍摄位置**；**为演示和时序预测而进行的连续监测，则需确保每次飞行尽量覆盖同一目标水体（同一池塘），保持中心 GPS 和高度基本一致**。必须记录每张图像的无人机飞行参数（高度、GPS、俯仰横滚偏航角、相机内参等），按「拍摄日期+天气+序号」命名图像及参数文件（如 `20240315_sunny_001.RAW` 和 `20240315_sunny_001.json`），分别归档至 `data/01_raw/images/` 和 `data/01_raw/drone_json/`。
- **几何校正执行**：调用 AI 模块提供的正射校正脚本 `utils/preprocess/orthorectify.py`，批量读取 `data/01_raw/` 中的原始图像与参数文件，输出几何校正后的正射影像 PNG 至 `data/02_preprocessed/images/`，同时生成包含 GSD 等信息的元数据文件至 `data/02_preprocessed/meta/`，文件名保持不变（扩展名改为`.png`/`.json`）。
- **像素级标注**：使用 LabelMe 对 `data/02_preprocessed/images/` 中的校正图像进行标注，生成 JSON 标注文件存入 `data/03_labeled/labels_json/`，掩码图存入 `data/03_labeled/masks/`。
- **数据标准化**：撰写数据规范文档，确保文件命名、参数格式、目录结构符合项目约定。在规范中明确说明：文件名所含的日期字段是构建时序序列的时间戳，天气字段是分割模型路由和数据集分类的关键标签。

**不涉及工作**：不参与深度学习模型训练（包括天气分类模型与分割模型）、浮游植物量化计算、数模预测、前端可视化开发、展示端对接。

---

### 🤖 3.2 AI视觉语义分割与量化模块（负责人：吴天宇）

**核心定位**：项目算法核心，负责模型训练、推理量化，并**为下游提供空间仿真脚本**。模型只做分割，面积在后处理中通过元数据计算。

**具体职责**

- **轻量标准化脚本提供**：编写并交付 `utils/preprocess/orthorectify.py`，功能为：
  - 校验飞行姿态（俯仰、横滚）是否在允许范围内，否则拒绝处理并提示重拍；
  - 校验白平衡模式是否为预设固定值，若为自动则拒绝并提示；
  - 记录并校验焦距是否与**基准值**一致(做一个开关，训练时传入false，用户端默认true)；
  - 假设图像为垂直正射，基于内参计算理论 GSD；
  - 将原图等比缩放至长边 ≤1024，居中黑边填充为 1024×1024 PNG；
  - 同步输出包含有效GSD、有效区域边界的元数据文件。
  - 注意：**本脚本不执行透视变换或视场畸变矫正**，所有矫正依赖严格的拍摄规范。
- **设计并交付无人机参数解析脚本**：编写 `parse_drone_metadata.py`，供标准化脚本`orthorectify.py`使用，确保 GSD 数据的有效提取。
- **天气分类模型构建**：使用 **多类天气图片数据集**（存放于 `data/weather_public/`）训练轻量级天气分类模型（ResNet-18），用于自动识别输入航拍图为“晴天”、“阴天”或“雾天”，权重保存至 `models/weather_classifier/best_weather_model.pth`。
- **多条件分割模型训练**：从 `data/03_labeled/` 读取图像与掩码，按文件名中的天气标签划分三个子数据集，分别训练三个独立的 DeepLabV3+ 语义分割模型，权重分别保存至 `models/deeplabv3plus/sunny/`、`cloudy/`、`hazy/`。**同时使用全部标注数据训练一个不区分天气的统一 DeepLabV3+ 模型，权重保存至 `models/deeplabv3plus/combined/`，并在训练过程中计算并记录 mIoU、Accuracy 等指标。**
- **推理与量化计算**：推理脚本 `models/predict_pipeline.py` 接收输入图像及对应元数据，自动调用天气分类模型，路由至对应 DeepLabV3+ 模型，输出分割掩码图至 `outputs/masks/`。**同时读取校正元数据中的空间分辨率信息，将像素面积转换为实际面积（平方米/亩）**，输出量化数据（JSON/CSV）至 `outputs/quantifications/`。
- **空间仿真脚本提供**：编写并交付 `utils/simulation/generate_prediction_mask.py`，实现基于基准掩码和预测面积变化率及**空间生长规则**（如扩张方向偏好、边缘权重等）的形态学膨胀/腐蚀逻辑。同时提供 `utils/simulation/extract_growth_pattern.py`，用于从两次实测掩码中提取斑块变化的统计特征。详细使用说明交付刘俊辉。
- **数据格式约定**：制定 `outputs/` 下数据的格式规范（掩码命名规则、量化 JSON 字段），确保展示组可直接读取。

**不涉及工作**：不参与图像采集、LabelMe 标注、数模模型构建、前端可视化开发、展示端部署。

---

### 📈 3.3 数模时序预测模块（负责人：刘俊辉）

**核心定位**：项目趋势预警核心，基于数学建模方法搭建双模型预测系统，完成未来面积推演与风险等级评估，并利用 AI 模块提供的空间仿真脚本生成对应的预测分布图。

**具体职责**

- **预测模型搭建与参数拟合**：
  - 接收吴天宇交付的浮游植物时序实测数据（从 `outputs/quantifications/` 获取）。
  - 搭建**两套**水华增长预测模型：
    - **统计基线模型**：ARIMA 时序预测，仅依赖历史面积数据进行统计拟合。
    - **机理增强模型**：基于 Logistic 生长方程，可融合水温、光照等环境变量。方程参数通过非线性最小二乘法对实测数据拟合确定。
  - 完成模型参数估计后，在同一数据集上对比两套模型的预测误差（RMSE/MAE），产出**模型对比评估表**。
- **默认参数设计**：编写《模型参数说明书》，为机理模型的所有参数（最大生长速率、半饱和常数、环境容纳量、水温、光照等）提供基于文献和地区气候统计数据的默认值及来源说明。
- **预测分布图生成**：
  - 调用吴天宇交付的 `utils/simulation/extract_growth_pattern.py`，从实测掩码序列中提取斑块生长的空间规律（扩张方向、速率、边缘偏好等）。
  - 将预测的未来面积值及提取的生长规则传入 `utils/simulation/generate_prediction_mask.py`，生成未来各日期的预测掩码图，按约定命名存入 `outputs/masks/`。
- **数据交付**：将预测数据及仿真掩码整理，确保预测量化 JSON 存入 `outputs/quantifications/`，并与实测数据格式对齐。

**不涉及工作**：不参与无人机图像预处理、标注、DeepLabV3+ 训练、前端可视化、展示端开发。

---

### 🎨 3.4 交互式可视化展示模块（负责人：乔梓阁）

**核心定位**：项目成果展示端，将算法、数模核心结果转化为可交互本地 Demo，模拟最终产品形态。

**具体职责**

- **展示端开发**：基于个人技术栈，实现本地可运行程序（大创阶段以完成演示功能为准）。
- **产品模拟交互**：实现“我的池塘”列表、创建池塘、按日期上传原始图像与无人机参数等界面。
- **上传一致性校验**：在“创建池塘”时记录基准GPS与飞行高度，后续上传新航拍图时自动校验位置和高度是否在允许偏差范围内。
- **预测设置 UI**：在“我的池塘”页面中实现**预测模型选择与参数配置**功能：
  - **简易模式（默认）**：使用统计基线模型，无需任何额外输入。
  - **精准模式**：使用机理增强模型，界面展示参数表格，系统预填基于该地区/季节的科学默认值（附来源提示），用户可手动修改或一键导入环境数据文件。
- **核心可视化功能**：从 `outputs/masks/` 和 `outputs/quantifications/` 读取数据，搭建时间进度条、“实测/预测模式”切换、分布图动态渲染，同步展示量化数据。采用固定底图 + 动态掩码叠加的方式。

**不涉及工作**：不参与数据采集、预处理、标注、AI 模型训练、数模预测计算。

---

## 🔗 4. 项目全流程执行链路（不可跳步）

1. 航拍组按规范完成水体无人机航拍（覆盖晴天/阴天/雾天），交付原始航拍图像与配套无人机参数文件给贺一冉。图像命名为 `YYYYMMDD_天气_序号.RAW`，参数文件同名 `.json`，分别存入 `data/01_raw/images/` 和 `data/01_raw/drone_json/`。
2. 贺一冉使用 AI 模块提供的正射校正脚本 `utils/preprocess/orthorectify.py`，批量处理 `01_raw/` 中的图像与参数，输出几何校正后的标准 PNG 图像至 `data/02_preprocessed/images/`，同步输出含 GSD 的元数据至 `data/02_preprocessed/meta/`，文件名保持不变（扩展名改为.png / .json）。
3. 贺一冉使用 LabelMe 对校正图像完成像素级标注，生成 JSON 标注文件存入 `data/03_labeled/labels_json/`，掩码图存入 `data/03_labeled/masks/`，交付吴天宇。
4. 吴天宇基于 `data/weather_public/` 中的公开数据集训练天气分类模型；基于 `data/03_labeled/` 完成**四个** DeepLabV3+ 模型训练（三个天气专属模型 + 一个不区分天气的统一对比模型），并在训练过程中统计各模型的 mIoU、Accuracy 等指标，产出对比评估表格；编写推理管道及空间仿真脚本（含规律提取工具），并交付使用说明给刘俊辉。
5. 吴天宇运行 `models/predict_pipeline.py`，对用于演示的测试图像及**配套元数据文件**执行推理，输出实测分割掩码图至 `outputs/masks/`，实测量化数据（**基于元数据中的 GSD 计算实际面积**）至 `outputs/quantifications/`，交付刘俊辉。
6. 刘俊辉从 `outputs/quantifications/` 读取实测时序数据，完成双模型**搭建与参数拟合**，输出模型对比评估表与标准化预测数值（存入 `outputs/quantifications/`）。随后调用吴天宇提供的规律提取脚本，从实测掩码序列中提取空间生长规则，再将面积变化率与生长规则一并传入 `utils/simulation/generate_prediction_mask.py`，为每个预测日期生成仿真掩码图，按约定命名存入 `outputs/masks/`。
7. 吴天宇确认 `outputs/` 下所有数据的格式符合事先约定的规范，通知乔梓阁数据就绪。
8. 乔梓阁基于展示端框架开发交互功能，从 `outputs/` 读取素材，完成 Demo 搭建（含池塘管理模拟界面及预测设置UI）。
9. 全团队联调测试，验证模块数据对接，准备结题答辩。

## 📦 5. 各模块核心交付物

### 🗂️ 5.1 数据采集与预处理标注模块（贺一冉）

- 原始无人机航拍图像（`data/01_raw/`）；
- 预处理干净图像数据集（`data/02_preprocessed/images/`）及元数据（`data/02_preprocessed/meta/`）；
- LabelMe 标注全量数据集（`data/03_labeled/`）；
- 数据规范文档。

### 🤖 5.2 AI视觉语义分割与量化模块（吴天宇）

- 正射校正脚本（`utils/preprocess/`）及使用说明；
- 天气分类模型权重及代码；
- 三个天气专属 DeepLabV3+ 模型权重及训练脚本；
- 一个统一 DeepLabV3+ 对比模型权重及训练脚本；
- 总推理脚本 `predict_pipeline.py`；
- 空间仿真增强脚本 `generate_prediction_mask.py` 及规律提取脚本 `extract_growth_pattern.py`；
- 实测分割掩码图与量化数据；
- `outputs/` 数据格式规范文档；
- 包含对比实验表格的模型评估报告。

### 📈 5.3 数模时序预测模块（刘俊辉）

- 双模型（ARIMA + Logistic 生长）完整代码文件；
- 模型对比评估表；
- 《模型参数说明书》（含默认值及来源说明）；
- 标准化预测数值数据（`outputs/quantifications/`）；
- 仿真生成的预测掩码图（`outputs/masks/`）；
- 水华爆发风险等级评估分析报告。

### 🎨 5.4 交互式可视化展示模块（乔梓阁）

- 本地可独立运行的展示端 Demo 程序；
- 展示端操作手册与演示流程文档。

---

## 📁 6. 项目文件夹结构（精简版，无冗余）
```
Phytoplankton_UAV_Project/
│
├── data/
│ ├── 01_raw/
│ │ └──  20240315_sunny_001.DNG      ← 直接放原图（RAW / DNG）
│ ├── 02_preprocessed/
│ │ ├── images/ # 校正后PNG图像（1024×1024）
│ │ └── meta/ # 校正元数据（含GSD）
│ ├── 03_labeled/
│ │ ├── labels_json/ # LabelMe标注JSON
│ │ └── masks/ # 像素掩码图
│ └── weather_public/ # 公开天气数据集
│ ├── sunny/
│ ├── cloudy/
│ └── hazy/
│
├── models/
│ ├── weather_classifier/
│ │ ├── train.py
│ │ └── best_weather_model.pth
│ ├── deeplabv3plus/
│ │ ├── sunny/
│ │ ├── cloudy/
│ │ ├── hazy/
│ │ └── combined/ # 统一对比模型
│ ├── forecast/ # 数模预测模型
│ │ ├── arima_model.py
│ │ ├── logistic_growth_model.py
│ │ ├── model_comparison.py
│ │ └── params_default.json
│ └── predict_pipeline.py # 总推理入口
│
├── utils/
│ ├── preprocess/
│ │ ├── orthorectify.py
│ │ └── parse_drone_metadata.py
│ │ └── brand_mapping.json
│ └── simulation/
│ ├── generate_prediction_mask.py
│ └── extract_growth_pattern.py
│
├── test_images/
├── outputs/
│ ├── masks/
│ └── quantifications/
│
├── requirements.txt
└── README.md
```



### 6.1 文件夹结构与使用逻辑说明

| 目录/文件                                     | 负责模块 | 读取来源 | 输出目标 |
|:------------------------------------------| :--- | :--- | :--- |
| `data/01_raw/`                     | 数据模块 | 航拍组采集 | 存放原始RAW图像 |
| `data/02_preprocessed/images/`            | 数据模块（执行） / AI模块（提供脚本） | `01_raw/` 中的图像+参数 | 校正、缩放、填充后的 1024×1024 PNG 图像 |
| `data/02_preprocessed/meta/`              | 数据模块（执行） / AI模块（提供脚本） | 校正缩放参数 | 记录每张图像的 GSD、有效区域边界，供面积计算使用 |
| `data/03_labeled/`                        | 数据模块 | 预处理图像 | JSON标注与掩码图，供AI模块训练分割模型 |
| `data/weather_public/`                    | AI视觉模块 | 从公开数据集下载 | 训练天气分类模型的图像数据 |
| `utils/preprocess/orthorectify.py`        | AI视觉模块 | `../../data/01_raw/` | `../../data/02_preprocessed/images/` |
| `utils/simulation/generate_prediction_mask.py` | AI视觉模块 | 基准掩码路径 + 面积变化率 | 未来日期预测掩码图（供数模组调用） |
| `models/weather_classifier/train.py`      | AI视觉模块 | `../../data/weather_public/` | 天气分类模型权重 |
| `models/deeplabv3plus/{weather}/train.py` | AI视觉模块 | `../../../data/03_labeled/` 中对应天气的图像与掩码 | 对应天气的 DeepLabV3+ 权重 |
| **【新增】`models/deeplabv3plus/combined/train.py`** | AI视觉模块 | `../../../data/03_labeled/` 中全部图像与掩码 | 统一 DeepLabV3+ 对比模型权重 |
| `models/predict_pipeline.py`              | AI视觉模块 | 输入图像路径 + 元数据JSON路径 | `outputs/masks/` 掩码图，`outputs/quantifications/` 量化JSON（含实际面积） |
| `outputs/`                                | AI视觉模块（输出）/ 数模模块（输出预测）/ 展示模块（读取） | AI推理结果 / 数模预测结果及仿真掩码 | 展示端直接读取，无需中间转换 |


### 6.2 文件命名规范

- **原始图像**：`YYYYMMDD_天气_序号.RAW`（如 `20240315_sunny_001.RAW`）
- **预处理图像**：`YYYYMMDD_天气_序号.png`（如 `20240315_sunny_001.png`）
- **校正图像元数据**：`YYYYMMDD_天气_序号_meta.json`（如 `20240315_sunny_001_meta.json`），存放于 `data/02_preprocessed/meta/`。
- **标注JSON**：`YYYYMMDD_天气_序号.json`（如 `20240315_sunny_001.json`）
- **掩码图**：`YYYYMMDD_天气_序号.png`（同名，存放于 `masks/` 子目录）
- **天气标签取值**：`sunny`（晴天）、`cloudy`（阴天）、`hazy`（雾天）

---

## 📅 7. 项目阶段规划（仅供参考）（大创12个月执行周期）

### 7.1 第一阶段：数据准备期（第1-2个月）
- 航拍组采集多天气图像及配套参数（训练数据可混合多水域，演示数据锁定同一池塘）；
- 贺一冉完成正射校正、全量 LabelMe 标注；
- 团队验收数据集。

### 7.2 第二阶段：算法模型开发期（第3-5个月）
- 吴天宇基于公开数据集训练天气分类模型，基于标注数据集训练三个 DeepLabV3+ 分割模型及一个统一对比模型；开发推理管道及空间仿真脚本；完成对比评估表格。
- 刘俊辉完成预测模型搭建；两组完成数据格式对齐。

### 7.3 第三阶段：展示端开发与集成期（第6-8个月）
- 乔梓阁完成展示端开发（含池塘管理模拟）；
- 全团队联调测试，完善演示流程。

### 7.4 第四阶段：结题验收准备期（第9-12个月）
- 整理代码、数据、文档；
- 优化展示端效果；
- 撰写结题报告；
- 模拟答辩。

---

## ⚖️ 8. 项目重要约定（核心执行规则）

### 8.1 数据格式统一约定

- **时间格式**：`YYYY-MM-DD`，文件名中日期部分为 `YYYYMMDD`。
- **浮游植物类别标签**：`microcystis`（微囊藻）、`anabaena`（水华鱼腥藻）、`spirogyra`（水绵）、`other_phyto`（其他浮游植物）、`clear_water`（清水）、`sediment`（泥沙）、`shadow`（阴影）。
- **天气标签**：`sunny`（晴天）、`cloudy`（阴天）、`hazy`（雾天）。
- **图像尺寸**：统一为 `1024×1024`。
- **文件格式**：结构化数据用 JSON/CSV，图像用 PNG，标注文件用 LabelMe 标准 JSON。
- **相机焦距**：必须在无人机 JSON 中提供 `camera.focal_length`，且数值应为相机实际焦距（换算到35mm等效焦距的说明见后）。连续监测同一池塘时，焦距需保持与首次上传基准值一致。
- **白平衡**：JSON 中需记录实际使用的白平衡模式，且必须为非自动模式。

### 8.2 分工边界约定

- 各模块仅负责自身职责，不得承接其他模块任务。
- 数据交付需附带说明文档，明确数据内容、格式、适用模块。
- 跨模块问题需 24 小时内沟通解决，不得擅自修改其他模块交付数据。

### 8.3 大创验收优先级约定

- 优先完成技术闭环与本地 Demo 演示，不追求每日拍摄、公网部署。
- 展示端演示采用「3-5 组真实实测数据 + 模拟预测数据」，重点展示功能流程。
- 答辩优先保障操作流畅性、功能完整性。

### 8.4 沟通与进度约定

- 每周开展 1 次团队同步会，汇报进度、协调对接。
- 核心节点提前 1 天通知对接模块，预留验收时间。
- 所有沟通、交付记录需留存归档。